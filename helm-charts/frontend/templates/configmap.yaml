apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-env
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  env.json: |
    {"env": {{ .Values.env | quote }}}
