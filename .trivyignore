# Namespace is assigned per environment by the ArgoCD Application
# (destination.namespace -> dev/test/prod). The charts are intentionally
# namespace-agnostic, so the static "default namespace" finding is a false positive.
KSV-0110

# Image trust is enforced at admission by Kyverno verifying the Cosign keyless
# signature of ghcr.io/svetlioo/* (stronger than a registry-name allowlist).
KSV-0125
